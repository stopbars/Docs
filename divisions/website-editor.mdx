---
title: "Website Editor"
---

Divisions using vatSys or another controller client other than EuroScope can maintain airport lighting data in the editor through [stopbars.com](https://stopbars.com). You can draw lighting objects, enter coordinates, and overlay a chart to help position the lighting objects.

## Before you start

Complete [division setup](/divisions/getting-started). Open your account on the BARS website, select **Manage Division**, then open the approved airport's editor through gear icon.

<Frame>
  <img src="/images/v2/Divisions-Open-Airport-Editor.png" alt="Approved YSSY airport card with the pointer over the gear icon that opens the airport editor" />
</Frame>

## Draw a lighting object

<Steps>
  <Step title="Create new object">
    Create a new object through the **+ Add New Object** button, click the map to place vertices along the lighting row. Add at least two vertices. Finish with **Ctrl + left-click** on the map or by clicking a vertex. Press **Escape** to cancel drawing.

    <Frame>
      <img src="/images/v2/Divisions-Create-New-Object.gif" alt="Animation demonstrating how to create a new lighting object and place its vertices on the map" />
    </Frame>
  </Step>
  <Step title="Set its properties">
    Choose the object type and enter its name. Set the directionality, colour, properties, etc

    <Frame>
      <img src="/images/v2/Divisions-Add-New-Object.png" alt="Add New Object panel showing the object type, name, directionality, additional properties, and Save button" />
    </Frame>
  </Step>
  <Step title="Check its position and orientation">
    Adjust the vertices to follow the positions of the lights. For unidirectional lights, inspect the displayed direction and use **Reverse** button to change the lights direction.
  </Step>
  <Step title="Save the object to your pending changes">
    Select **Save**, repeat for the other lighting objects you need to add or change.
  </Step>
</Steps>

<Warning>
  Saving an object's properties does not upload the airport changes. Finish with **Review Changes** and **Save & Upload** before leaving the editor.
</Warning>

## Enter coordinates when imagery is outdated

You can place lighting at known coordinates even when the background map does not show recent changes at the airport.

1. Start a new object and select **Vertices by Coordinates**.
2. Enter each vertex in order, using latitude followed by longitude. Decimal coordinates such as `-27.3815, 153.1314` and degrees, minutes, and seconds are supported. This example only illustrates the format.
3. Use **Add Vertex** for additional points and provide at least two valid coordinates within the airport boundary.
4. Select **Map Coordinates**, check the shape on the map, then select **Next Step** to complete the object's properties.
5. Save it to the pending changes, then review and upload when ready.

If coordinates are rejected, check their format, latitude and longitude order, and hemisphere signs.

<Frame>
  <img src="/images/v2/Divisions-Vertices-By-Coordinates.png" alt="Add New Object panel with two latitude and longitude vertices, Coordinates Mapped confirmation, and an enabled Next Step button" />
</Frame>

## Overlay an airport chart

The **Image Overlay** panel lets you place a chart image over the map. Use a PNG, JPEG, WebP, or GIF image. Export a PDF chart page to an image before uploading it here.

<Steps>
  <Step title="Upload the image">
    Select **Upload** in Image Overlay and choose your chart image.
  </Step>
  <Step title="Position the chart">
    Move, resize, and rotate it to match the airport. Lower **Opacity** so you can compare the chart with the map underneath.
  </Step>
  <Step title="Align matching landmarks">
    For **Auto Align**, select **Start**. Click a recognisable point on the overlay, then the same location on the basemap. Repeat with at least one more landmark pair, then select **Apply Alignment**.

    Choose landmarks whose positions have not changed. Check the alignment across the area you intend to edit.
  </Step>
  <Step title="Lock the overlay and add lighting">
    Lock the overlay position to avoid moving it while working. Draw or adjust the lighting objects using the chart as a reference.

    <Frame>
      <img src="/images/v2/Divisions-Locked-Chart-Overlay.png" alt="Airport chart overlaid on satellite imagery with lighting objects and the Image Overlay panel showing a locked padlock icon" />
    </Frame>
  </Step>
</Steps>

## Review and upload changes

Select **Review Changes** to inspect additions, modifications, and deletions. Check each changed object, including its name, type, shape, and directionality.

Select **Save & Upload** when the changes are ready. Wait for the **Changes Saved** confirmation and check the refreshed objects. New objects now have their BARS IDs.

If the upload fails, read the error and resolve it before leaving the page. Do not assume that a failed upload or an object-level save has applied your changes to BARS.

## Link lead-on lights to stopbars

Upload the stopbar and lead-on objects before linking them.

1. Select an existing stopbar.
2. In **Lead-on links**, select **Click Lead-ons**.
3. Click the lead-on objects on the map to select or deselect their links to this stopbar.
4. Select **Save** in the Lead-on links section and wait for **Lead-on Links Saved**.

Lead-on links save separately from the pending geometry and property changes. A lead-on can link to more than one stopbar, so check the selection against the intended airport configuration.

## Finish airport setup

Once all lighting data and lead-on links are checked, return to the division management page for your division, and [enable scenery contributions](/divisions/management#control-scenery-contributions), this will allow contributors to begin contributing.
